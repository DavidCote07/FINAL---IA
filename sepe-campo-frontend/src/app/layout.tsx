import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'SEPE Campo - Sistema de Verificación de Obra Eléctrica',
  description: 'Sistema para gestionar visitas de verificación de obra eléctrica con generación de informes y exportación a Excel',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="es">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
